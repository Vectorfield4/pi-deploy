# TimeWeb AI Gateway (DeepSeek V4)
TIMEWEB_API_KEY=

# dense-mem PostgreSQL — auto-generated by make init if empty
POSTGRES_PASSWORD=

# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# GitHub
GITHUB_TOKEN=

# Vercel (optional)
VERCEL_TOKEN=
VERCEL_ORG_ID=

# FTP (optional)
FTP_HOST=
FTP_USER=
FTP_PASS=

# Context7 (optional — works without key at IP-based rate limits)
CONTEXT7_API_KEY=

# dense-mem
# Control portal token — auto-resolved by memory-bootstrap.sh if not set
# (tries docker container, then control portal default). Set manually if preferred.
CONTROL_PORTAL_TOKEN=
# DENSE_MEM_API_KEY — auto-filled by running: bash scripts/memory-bootstrap.sh
# (requires dense-mem stack running and CONTROL_PORTAL_TOKEN available)
DENSE_MEM_API_KEY=
# Host port that maps to dense-mem's container port 8090. Change if 8090 is busy.
DENSE_MEM_PORT=8090
# Team name for dense-mem (used by memory-bootstrap.sh)
TEAM_NAME=pi-coder
# Credential name for dense-mem API key (used by memory-bootstrap.sh)
CREDS_NAME=default

# AI Verifier (required for claim verification in dense-mem)
# Verifier validates claims against stored evidence using an LLM.
# Defaults shown are for Ollama — change if using OpenAI-compatible provider.
AI_VERIFIER_API_URL=http://host.docker.internal:11434/v1
AI_VERIFIER_API_KEY=ollama
AI_VERIFIER_MODEL=llama3.1:8b
AI_VERIFIER_TIMEOUT_SECONDS=300
