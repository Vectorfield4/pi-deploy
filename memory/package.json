{
  "name": "pi-pgvector-api-embeddings",
  "version": "0.1.0",
  "description": "Lightweight MCP memory server: Postgres+pgvector with remote API embeddings. Replaces dense-mem.",
  "type": "module",
  "main": "dist/index.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.0"
  }
}
